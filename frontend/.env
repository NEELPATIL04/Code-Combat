# Backend Configuration
# Set VITE_BACKEND_MODE to switch between local and live backend
# - local = Development mode (Vite proxy to http://localhost:5000)
# - live = Connect to live server (http://49.13.223.175:5000)

# Switch to 'live' to connect to production server from local development
VITE_BACKEND_MODE=live

# Backend URLs (defaults are set, but you can override)
VITE_LOCAL_BACKEND_URL=http://localhost:5000/api
VITE_LIVE_BACKEND_URL=http://49.13.223.175:5000/api
